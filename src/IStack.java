/**
 * Created by coag on 16-02-2017.
 */
public interface IStack {
    Integer size = 0;

    // now this inteface has comments
    boolean push(int element);
    int pop();
    int peak();
    int size();
    boolean clear();
    //Eduards is happy now
}
